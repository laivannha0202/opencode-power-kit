# v1.2.0 — opk CLI / Setup

**Date:** 2026-05-31

## Highlights

- **`setup.sh`** — interactive menu with 7 options + 7 non-interactive flags
- **`bin/opk`** — CLI wrapper (help, version, path, global, install, etc.)
- **`install-global.sh`** — auto-installs `opk` to `~/.local/bin/opk`
- **Improved PATH handling** — ensures `~/.local/bin` in PATH
- **Dynamic pack report** — `GLOBAL_PACK_REPORT.md` generated with actual content

## Backward Compatible

100% backward compatible with v1.1.1.
