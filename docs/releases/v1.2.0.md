# v1.2.0 — opk CLI / Setup

**Ngày:** 2026-05-31

## Điểm nổi bật

- **`setup.sh`** — menu tương tác với 7 options + 7 non-interactive flags
- **`bin/opk`** — CLI wrapper (help, version, path, global, install, v.v.)
- **`install-global.sh`** — tự cài `opk` vào `~/.local/bin/opk`
- **Cải thiện PATH handling** — đảm bảo `~/.local/bin` trong PATH
- **Dynamic pack report** — `GLOBAL_PACK_REPORT.md` sinh với nội dung thực tế

## Tương thích ngược

100% tương thích ngược với v1.1.1.
