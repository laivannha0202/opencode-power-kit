# v1.3.1 — Hardening + CI

**Ngày:** 2026-06-05

## Điểm nổi bật

- **Ghim phiên bản BMAD_METHOD_VERSION** — mặc định 6.8.0, env override
- **Full log capture** cho BMAD install/update
- **Chuỗi OPK_USER_NAME** — user name từ env → git config → system
- **`update-bmad.ps1`** — Windows parity
- **CI strict** — shellcheck/shfmt không còn cho phép failures
- **shfmt canonical style** áp dụng cho toàn bộ 15 shell scripts
- **Hardened identity** — không còn hardcode `--user-name nha` ở bất kỳ đâu

## Tương thích ngược

100% tương thích ngược. Tất cả scripts/flags hiện có hoạt động không đổi.
