# v1.3.1 — Hardening + CI

**Date:** 2026-06-05

## Highlights

- **BMAD_METHOD_VERSION pin** — default 6.8.0, env override
- **Full log capture** for BMAD install/update
- **OPK_USER_NAME chain** — user name from env → git config → system
- **`update-bmad.ps1`** — Windows parity
- **CI strict** — shellcheck/shfmt no longer allow failures
- **shfmt canonical style** applied to all 15 shell scripts
- **Hardened identity** — no `--user-name nha` hardcode anywhere

## Backward Compatible

100% backward compatible. All existing scripts/flags work unchanged.
