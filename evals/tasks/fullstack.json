[
  {
    "id": "fullstack-001",
    "name": "API endpoint with validation",
    "description": "Agent should create a REST endpoint with proper validation",
    "input": "Create a POST /api/products endpoint in Express.js that: (1) validates the request body (name required, price > 0, description optional), (2) saves to database, (3) returns the created product with 201 status. Include input validation.",
    "expected_contains": ["POST", "validate", "status", "201"],
    "expected_not_contains": ["eval(", "exec("],
    "max_tokens": 4000,
    "timeout_seconds": 120
  }
]
